# frameworks/gates/portable-shell-baseline.txt
#
# DATED, ENUMERATED EXEMPTIONS for frameworks/gates/test-portable-shell.sh. Every line here is a
# KNOWN DEBT the fence measured against origin/main on 2026-09-20 (5 findings across 4 files, keyed
# to 4 entries) and is permitted to keep finding until it is fixed. Nothing else is permitted: a
# finding not listed here fails the suite.
#
# THIS IS A DEBT LEDGER, NOT AN EXEMPTION MECHANISM. Two properties make it shrink rather than
# accumulate:
#   1. A finding NOT listed here is a hard failure. New unportable spellings cannot land.
#   2. A line listed here that NO LONGER TRIPS is ALSO a hard failure ("stale baseline entry").
#      So fixing a site forces deleting its line in the same PR — the ledger cannot silently
#      outlive the debt and quietly re-exempt the same defect in the same file years later.
# Property 2 is the half that is usually missing, and the half that does the work: without it an
# "exemption list" only ever grows, and a fixed defect keeps a standing invitation to come back.
#
# FORMAT: <repo-relative path>@@<the rule's full "why" text, verbatim>
# Keyed on path + rule CLASS, deliberately NOT on line number: an unrelated PR that inserts a line
# above a known site would otherwise red for a defect it did not introduce. The cost is that a
# second instance of the SAME rule in an ALREADY-listed file is covered by the existing entry —
# accepted, because that file is already on the ledger and slated for a fix. That is why 5 findings
# reduce to 4 lines: self-check.yml trips the same rule twice.
#
# ─── WHY THERE IS A BASELINE AT ALL ───────────────────────────────────────────────────────────
# The fence fixes NOTHING. A fence PR that also rewrites the code it fences cannot demonstrate that
# the fence is what caught it — the diff would show the repairs and the reader would have to take
# the detection on trust. So every finding measured on main is enumerated here instead, and the
# fence lands live and blocking for everything else from the moment it merges.
#
# ─── GROUP A: covered by PR #95 (OPEN, unmerged as of 2026-09-20) ─────────────────────────────
# 3 findings in the 3 shell files #95 rewrites. They still trip on main TODAY, so they still belong
# here. Delete these three lines in #95 — the stale-entry check will demand it the moment it lands,
# which is exactly the mechanism working.
#
# (#95 also fixes .github/workflows/image-content-gate.yml: `"${args[@]}"` on a zero-element array
# under `set -u` in bash <4.4. That is the same bash-3.2 class but needs array-shape analysis, not
# a spelling match, so this fence has no rule for it and it is not listed here. Said plainly rather
# than left as an unexplained gap between #95's file list and this ledger.)
#
# run-local.sh:109 and verify-receipt.sh:53 — `mapfile` reading the gate registry.
frameworks/gates/scripts/run-local.sh@@`mapfile`/`readarray` are bash 4+; /bin/bash on macOS is 3.2.57
frameworks/gates/scripts/verify-receipt.sh@@`mapfile`/`readarray` are bash 4+; /bin/bash on macOS is 3.2.57
#
# escalation-handler.sh:12 — `declare -A SEVERITY_CHANNELS`. On 3.2 this is NOT an error: it makes
# an ordinary indexed array, every keyed write collapses onto index 0, and the severity→channel
# lookup answers every severity with one channel while exiting 0. Silent, not loud.
frameworks/workflow-engines/engine/escalation-handler.sh@@`declare -A`/`local -A` (associative arrays) are bash 4+; /bin/bash on macOS is 3.2.57, with no portable equivalent
#
# ─── GROUP B: NOT covered by any PR in flight ─────────────────────────────────────────────────
# 2 findings in 1 file, both in a YAML `run:` body — neither is a `.sh` file, and a `.sh`-only lint
# would never have seen either. They are the argument for porting the YAML widening rather than
# just the `.sh` rules. This needs its own PR; none is fixed here.
#
# self-check.yml:234 and :297 deserve a specific callout: those two `mapfile` calls are the
# DISCOVERY LOOPS of this repo's own gate-self-test and node-suite jobs — including the very loop
# that discovers and runs THIS fence. On a host bash they fail, and the comment already beside :294
# notes that `mapfile < <(git ls-files …)` cannot propagate the lister's exit status. A discovery
# loop that cannot enumerate is the exact fail-open shape this fence exists to close, sitting one
# level above the fence. (PR #96 seeds this file's token-budget baseline row; it does not touch the
# `mapfile` calls, so this entry is not covered by it either.)
.github/workflows/self-check.yml@@`mapfile`/`readarray` are bash 4+; /bin/bash on macOS is 3.2.57
#
# ─── NOT ON THIS LEDGER, ON PURPOSE ───────────────────────────────────────────────────────────
# scripts/check-claude-config.sh:112 uses `readlink -f`, and the reference fence this was ported
# from BASELINES its counterpart there as the repo's one GNU/BSD finding. It is not listed here
# because it is not a finding here: that site guards the call with a `python3` realpath fallback on
# the NEXT line, which is correct code, and this port widened the counterpart search from the
# matched line to the enclosing statement group so it is seen. See the lib header's "TWO FALSE
# POSITIVES NOT IMPORTED", and drills (f)/(g) in test-portable-shell-drills.sh, which pin both that
# the widened guard matches a next-line fallback AND that it stays bounded.
