name: bad-rule4-release-cancel-true
on:
  push:
    tags:
      - "v*"
concurrency:
  group: bad-rule4-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
