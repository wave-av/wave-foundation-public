name: good-clean
on:
  pull_request:
concurrency:
  group: good-clean-${{ github.ref }}
  cancel-in-progress: true
jobs:
  build:
    runs-on: blacksmith-2vcpu-ubuntu-2404
    timeout-minutes: 10
    steps:
      - run: echo ok
