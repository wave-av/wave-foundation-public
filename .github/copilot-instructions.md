# GitHub Copilot instructions

See **`AGENTS.md`** for how to work in this repository — conventions, the validation gates that run
on every commit and PR, and the "consumed, not copied" model. **`CLAUDE.md`** has Claude-Code-specific
notes. Keep changes passing the pre-commit suite (`pre-commit run --all-files`).
